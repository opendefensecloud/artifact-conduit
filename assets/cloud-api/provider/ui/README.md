# UI assets

These manifests surface ARC in the platform-mesh marketplace portal.

A `ProviderMetadata` resource registers ARC as a provider tile (display name,
description, icon, support links). `ContentConfiguration` resources define the
bundled generic UI for each resource — list views, detail views, and create
modals wired into the marketplace's Luigi-based navigation.
